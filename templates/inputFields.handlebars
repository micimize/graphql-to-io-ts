{{#each this}}
{{#if description }}{{ multilineComment description }}
{{/if}}{{ name }}{{#unless isRequired}}?{{/unless}}: {{ subtypeOrPrimitive type subtype='.I' }}{{#if isArray}}[]{{/if}}{{#unless isRequired}} | null{{/unless}}
{{/each}}

{{ multilineComment description }}
export enum {{name}}Enum {
  {{#each values }}{{value}} = '{{ value }}',{{#unless @last}}
  {{/unless}}{{/each}}
}


{{ multilineComment description }}
const {{ name }} = createEnum<{{name}}Enum>({{name}}Enum, '{{ name }}')
type {{ name }} = t.TypeOf<typeof {{ name }}>

namespace {{ name }} {
  export type O = {{name}}Enum
}

export { {{ name }} }
